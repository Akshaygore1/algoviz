import Content from "./_components/page-content";

const TITLE = "Arrays & Hashing Visualized: 9 Interview Problems Step by Step";
const DESCRIPTION =
  "Watch Two Sum, Group Anagrams, Top K Frequent, Product Except Self, Valid Sudoku and more run one step at a time, with the live hash map, highlighted code and a plain-language reason for every move.";

export const metadata = {
  title: TITLE,
  description: DESCRIPTION,
  openGraph: { title: TITLE, description: DESCRIPTION },
};

export default function Page() {
  return <Content />;
}
